import 'package:flutter/material.dart';

class Bookatable extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
        child: Center(
          child: Icon(Icons.headset,
              size: 150.0,
              color: Colors.redAccent),
        )
    );
  }
}